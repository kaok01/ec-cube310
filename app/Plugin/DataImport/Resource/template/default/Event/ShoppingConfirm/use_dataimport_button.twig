<h2 class="heading02">ポイント利用</h2>
<div id="dataimport_box" class="column">
    <div id="dataimport_box__body" class="text-left form-group">
        <p id="dataimport_box__info" class="text-left">
            現在の保有ポイントは「<strong class="text-primary">{{ (dataimport.current) >= 0 ? (dataimport.current)|number_format  : 0 }} pt</strong>」です。<br/>
            ポイントは商品購入時に1pt＝<strong class="text-primary">{{ dataimport.rate|price }}</strong>として利用することができます。
            <a id="confirm_box__use_dataimport_edit_button" href="{{ url('dataimport_use') }}" class="btn btn-default btn-sm">ポイントを利用する</a>
        </p>
    </div>
</div>
