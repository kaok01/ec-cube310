<p class="txt_center">
    現在の保有ポイントは「<span class="text-primary">{{ dataimport.current >= 0 ? dataimport.current|number_format  : 0 }} pt</span>」です。<br/>
    {% if app.debug %}現在の仮ポイントは「<span class="text-primary">{{ dataimport.pre|number_format }} pt</span>」です。<br/>{% endif %}
    ※1pt＝<span class="text-primary">{{ dataimport.rate|price }}</span>でご利用いただけます。
</p>

